{
  "name": "orbitforge-core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared provider and agent orchestration runtime for OrbitForge.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*",
    "package.json"
  ],
  "scripts": {
    "build": "tsc -p .",
    "test": "npm run build && node --test dist/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "typescript": "^5.3.3"
  }
}
